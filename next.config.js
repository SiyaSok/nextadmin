/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "i2.wp.com",
      "www.cameo.com",
      "mir-s3-cdn-cf.behance.net",
    ],
  },
};

module.exports = nextConfig;
